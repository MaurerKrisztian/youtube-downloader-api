export class Constants {

    static CHATBOT_NAME = "[chatbot]"
    static MESSAGE = {
        SEND_MESSAGE: 'message', // server send a message
        MESSAGE_LISTENER: 'chatMessage' // ui send a message by this
    }
    static JOIN_ROOM = 'joinRoom'
    static ROOM_USERS = 'roomUsers' // update room users

}
